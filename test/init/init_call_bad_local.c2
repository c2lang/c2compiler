module test;

type Foo struct {
    i32 v;
}

fn void Foo.init(Foo* f, i32 v) { f.v = v; }

fn void test1() {
    Foo foo.init(1);  // This one is OK
    static Foo foo1.init(1);  // @error{static local variables cannot have an init call}
}
