// @warnings{no-unused}
module test;

type Type struct {
    i32 member;
}

fn void Type.init(Type n) {
}

fn void myfunc() {
    Type t;
    // this is no longer an error but an init function should have a side effect on its argument
    // hence should take a pointer.
    // the compiler might complain that t is passed by value but is uninitialized
    t.init();       // not an error{cannot access static type-function through variable}
}

