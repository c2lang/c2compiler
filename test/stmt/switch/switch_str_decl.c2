// @warnings{no-unused}
module test;

fn const char* test1() {
    switch (const char* str = "apple") {
    case "pear":
        return "";
    default:
        return str;
    }
}

fn const char* test2() {
    switch (const char* str) { // @error{variable declaration in switch expression must have an initializer}
    case "pear":
        return "";
    default:
        return str;
    }
}

