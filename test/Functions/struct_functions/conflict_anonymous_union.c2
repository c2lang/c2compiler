// @warnings{no-unused}
module test;

type Type struct {
    union {
        int32 x;
        int32 y;    // @error{member 'y' conflicts with struct-function 'test.Type.y'}
    }
}

func void Type.y(Type* t) {     // @note{previous declaration is here}
}

