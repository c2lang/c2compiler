// @warnings{no-unused}
module test;

func void print(const char* format, ...) @(printf_format=1) { }

func void test1() {
    print("%p");   // @error{too many format specifiers or not enough arguments}
}

func void test2() {
    print("%p %u", nil);   // @error{too many format specifiers or not enough arguments}
}

