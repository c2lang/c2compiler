// @warnings{no-unused}
module test;

static_assert(3, 4);    // @error{static_assert failed: 3 == 4}
static_assert(0xff, 0b11111110);    // @error{static_assert failed: 0xFF == 0b11111110}

type S struct {
    i32 x, y;
}

type T struct {
    u32 x;
}

static_assert(sizeof(S), 4);    // @error{static_assert failed: sizeof(S) == 4, sizeof(S): 8}
static_assert(4, sizeof(S));    // @error{static_assert failed: 4 == sizeof(S), sizeof(S): 8}
static_assert(sizeof(T), sizeof(S));    // @error{static_assert failed: sizeof(T) == sizeof(S), sizeof(T): 4, sizeof(S): 8}
