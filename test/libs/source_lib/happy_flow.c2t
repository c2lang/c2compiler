// @recipe bin
    $warnings no-unused
    $backend c no-build
    $use test_source_lib source

// @file{file1}
module test;

import foo;

public fn i32 main(i32 argc, const char** argv) {
    return foo.test4(argc);
}

// @expect{atleast, cgen/build.c}

static int foo_test4(int a);
static int foo_internal(int a);

static int foo_test4(int a)
{
   return 4 * foo_internal(a);
}

static int foo_internal(int a)
{
   return a + 1;
}

