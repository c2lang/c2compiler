// @warnings{no-unused}

module test;

#if ARCH_32BIT
static_assert(4, sizeof(isize));
static_assert(4, sizeof(usize));
#else
static_assert(8, sizeof(isize));
static_assert(8, sizeof(usize));
#endif

