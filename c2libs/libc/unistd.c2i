module unistd;

type size_t int32;

func uint32 sleep(uint32 __seconds);
func char* getcwd(char* buf, size_t size);
func int32 chdir(const char* path);

const int32 R_OK = 4;
const int32 W_OK = 2;
const int32 X_OK = 1;
const int32 F_OK = 0;

