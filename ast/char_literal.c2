/* Copyright 2022 Bas van den Berg
*/

module ast;

import ast_context;
import utils local;

public type CharLiteralBits struct {
    u32 : NumExprBits;
    u32 value : 8;
}

public type CharLiteral struct {
    Expr expr;
} @(opaque)

public func CharLiteral* CharLiteral.create(ast_context.Context* c, SrcLoc loc, u8 val) @(inline) {
    CharLiteral* b = c.alloc(sizeof(CharLiteral));
    b.expr.init(ExprKind.CharLiteral, loc, 1, 1, 0);
    b.expr.stmt.charLiteralBits.value = val;
    return b;
}

public func void CharLiteral.print(const CharLiteral* b, StringBuffer* out, u32 indent) {
    out.indent(indent);
    out.print("CharLiteral '%c'\n", b.expr.stmt.charLiteralBits.value);
}

