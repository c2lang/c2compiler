module working;

import stdio;

type Number i32;

public const char* nName = "Hallo";

i32[] numbers = { 1, 1, 2, 3, 5, 8, 13 }

type Buffer struct {
    i32 size;
    u8* data;
    u16 count;
}

public func void foo(i32 a, const char* text) {
    stdio.printf("foo %d %s\n", a, text);
}

public func i32 main(i32 argc, i8** argv) {
    Buffer buf;

    if (argc == 1) {
        stdio.printf("Hello World!\n");
    } else {
        stdio.printf("Goodbye!\n");
    }
    return 0;
}

