
shell.module: shell_cmd

shell.commands:
    git_describe:
        variable: git_describe
        path: .
        cmd: git describe --always --tags --dirty
        timeout: 2

    date:
        variable: date
        cmd: date
        path: .

    foobar:
        cmd: echo bla

