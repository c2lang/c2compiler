package test;

func void main() {
    // --- var decls, OK ---
    const char[12] buffer;
    int a;
    Number num;
    int* ptr;
    Number** ptr;
    int*[5][] bla;
    a b;

    // --- var decls with init, OK ---
    int a = 10;
    int[] a = { 10, 20 }
    Number[] num = { 10, 20 }
    int a = calc();
    int b = 10 / 2;
    int[][] b = a;
    Number a = bliep();
    int* ptr = NULL;
    Number** ptr = NULL;
    int*[10] bla;
    int[2] nums = { calc(), calc() }
    int b = 4/2;
    int b = Utils::max + Utils::Min;
    int x = Utils::getMax();


    // ERROR on STAR
    int b = 2*2;

    // --- assignment expression ---
    a = 10;
    res = calc();
    *bliep  = 0;       // ok, bliep -> primary_expression
    b *= 2;        // ok -> primary_expression
    text = "hallo" "dit" "is" "een" "test";

    // error on LSQUARE
    *array[2] = 'b';

    // error on STAR
    b = 2 * 20;

    // ERROR, b -> type_specifier WRONG, [2] -> array_specifier, ok
    b[2] = 0;
    b->foo = 10;
}

