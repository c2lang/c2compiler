package test;

type Number int;

func void main() {
    // --- var decls, OK ---
    const char[12] buffer;
    int a;
    Number num;
    int* ptr;
    Number** ptr2;
    int*[5][] bla;
    a b;    // error a is not a type

    // --- var decls with init, OK ---
    int a2 = 10;
    int[] a3 = { 10, 20 }
    Number[] num2 = { 10, 20 }
    int a4 = calc();
    int b2 = 10 / 2;
    int[][] b3 = a;
    Number a5 = bliep();
    int* ptr3 = NULL;
    Number** ptr4 = NULL;
    int*[10] bla2;
    int[2] nums = { calc(), calc() }
    int b4 = 4/2;
    int b5 = Utils::max + Utils::Min;
    int x1 = Utils::getMax();


    // ERROR on STAR
    int b6 = 2*2;

    // --- assignment expression ---
    a = 10;
    res = calc();
    *bliep  = 0;       // ok, bliep -> primary_expression
    b *= 2;        // ok -> primary_expression
    text = "hallo" "dit" "is" "een" "test";

    // error on LSQUARE
    *array[2] = 'b';

    // error on STAR
    b = 2 * 20;

    // ERROR, b -> type_specifier WRONG, [2] -> array_specifier, ok
    b[2] = 0;
    b->foo = 10;
}

