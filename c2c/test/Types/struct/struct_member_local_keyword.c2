// @warnings{no-unused}
module test;

type Point struct {
    int x;
    local int y;        // @error{cannot use keyword local for struct members}
    union {
        int a;
        local int b;        // @error{cannot use keyword local for union members}
    }
}

